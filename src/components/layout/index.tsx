import React from 'react'
import TabBar from '../Tabbar'

const WingBlank = () => (
  <div style={{boxSizing: 'border-box', height: '100%'}}>
    <TabBar />
  </div>
)

export default () => {
  return <WingBlank />
}
